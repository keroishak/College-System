@model IEnumerable<College_System.Models.AcademicYear>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AcademicYearID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AcademicYearID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AcademicYearID })|
            @Html.ActionLink("Semesters", "Index", "Semester", new { id = item.AcademicYearID, name = "academicyear" }, null)|
            @Html.ActionLink("Sections", "Index", "AcademicYearSection", new { id = item.AcademicYearID, name = "academicyear" }, null)|
            @Html.ActionLink("Departments","Index", "Department", new { id = item.AcademicYearID, name="academicyear" },null)|
            @Html.ActionLink("Students","Index", "Student", new { id = item.AcademicYearID, name="academicyear" },null)|
            @Html.ActionLink("Courses", "Index", "Courses", new { id = item.AcademicYearID, name = "academicyear" }, null)
        </td>
    </tr>
}

</table>
