@model IEnumerable<College_System.Models.Semester>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AcademicYear.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AcademicYear.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SemesterID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SemesterID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SemesterID }) |
            @Html.ActionLink("Courses", "Index", "Courses", new { id = item.SemesterID, name = "semester" }, null)
        </td>
    </tr>
}

</table>
